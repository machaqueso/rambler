
@{
    ViewData["Title"] = "External Accounts";
}

<link rel="import" href="/components/xenon-photo-card/xenon-photo-card.html" />
<link rel="import" href="/components/paper-toast/paper-toast.html" />
<link rel="import" href="/components/iron-ajax/iron-ajax.html" />

<dom-module id="page-module">
    <template>
        <iron-ajax id="ajax"
                   auto
                   url="/api/user/@ViewData["UserId"]/externalAccount"
                   handle-as="json"
                   loading="{{loading}}"
                   last-response="{{items}}"
                   debounce-duration="300"></iron-ajax>

        <xenon-photo-card heading="@ViewData["Title"]">
            <main>
                <div class="row">
                    <a href="@Url.Action("Index")" tabindex="-1">
                        <paper-button>Users</paper-button>
                    </a>
                </div>
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th>Source</th>
                            <th>Reference Id</th>
                            <th>Username</th>
                        </tr>
                    </thead>
                    <tbody>
                        <template is="dom-repeat" items="{{items}}">
                            <tr>
                                <td>{{item.apiSource}}</td>
                                <td>{{item.referenceId}}</td>
                                <td>{{item.username}}</td>
                            </tr>
                        </template>
                    </tbody>
                </table>
            </main>
        </xenon-photo-card>
        <paper-toast id="toast"></paper-toast>
    </template>
    <script>
        Polymer({
            is: "page-module",
            properties: {
            },
            observers: [
            ],
            ready: function () {
            }
        });
    </script>
</dom-module>

<page-module></page-module>

